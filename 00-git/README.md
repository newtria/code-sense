# 00. Git 버전 관리

## 이 폴더에서 배우는 것

- Git의 핵심 멘탈 모델 (스냅샷, 3개 영역)
- 일상적으로 쓰는 명령어
- 브랜치 전략과 협업 워크플로우
- 되돌리기 (실수 복구)

## 실습

```bash
cd examples
# 빈 레포를 만들고 직접 실습
bash git-playground.sh
```

## 핵심 질문

1. `git add`와 `git commit`이 분리되어 있는 이유는?
2. `git merge`와 `git rebase`의 차이는?
3. 실수로 커밋한 것을 되돌리는 3가지 방법은?
4. `.gitignore`에 넣어야 하는 파일의 기준은?
